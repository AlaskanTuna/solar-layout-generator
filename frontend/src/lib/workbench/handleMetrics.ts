/** Visual metrics for the single-panel rotation handle (distance from panel edge, hit-circle radius). */
export const ROTATION_HANDLE_METRICS = {
  distance: 24,
  radius: 8
} as const

/** Visual metrics for the multi-panel group rotation handle. Slightly larger radius for easier grabbing. */
export const GROUP_ROTATION_HANDLE_METRICS = {
  distance: 24,
  radius: 9
} as const
