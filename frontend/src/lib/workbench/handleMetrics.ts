/**
 * Defines the ROTATION_HANDLE_METRICS constant
 */
export const ROTATION_HANDLE_METRICS = {
  distance: 24,
  radius: 8
} as const

/**
 * Defines the GROUP_ROTATION_HANDLE_METRICS constant
 */
export const GROUP_ROTATION_HANDLE_METRICS = {
  distance: 24,
  radius: 9
} as const
